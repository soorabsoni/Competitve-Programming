#include<bits/stdc++.h>
using namespace std;

class TreeNode {
public:
    int val;
    TreeNode *left, *right;

    TreeNode(int data) : val(data), left(NULL), right(NULL) {}
};

int main() {
    TreeNode *root = new TreeNode(1);
    return 1;
}
